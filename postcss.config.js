// Tailwind CSS v4 — the plugin handles @import inlining, nesting and
// vendor prefixing internally, so postcss-import/nesting/autoprefixer are
// no longer needed.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
